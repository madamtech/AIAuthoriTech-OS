# AI AuthoriTech OS for Gemini

Use `catalog/knowledge-index.json` to locate assets and `catalog/assets.json` for authoritative governance data. Load only the skill folders needed for the current request. Follow each selected `SKILL.md` and its explicitly referenced supporting files.

Preserve permanent identifiers, privacy boundaries, guardrails, and source provenance. Do not claim behavioral testing from static validation. Treat bookmarked GPT records as authorized external adapters unless their Builder configurations have been captured and validated.
